class Solution {
public:
    vector<int> shuffle(vector<int>& nums, int n) {
        vector<int> v;
        int i,j=n;
        for( i=0;i<n;i++)
        {
            v.push_back(nums[i]);
            v.push_back(nums[j]);
            j++;
        }
        return v;
    }
};
